---
version: '3.8'

services:
  moto:
    image: motoserver/moto:5.0.0
    container_name: infra-diff-moto-test
    ports:
      - "50000:5000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
